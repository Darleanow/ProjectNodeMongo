extends layout

block content
  .error-container(style="text-align: center; padding: 50px 20px;")
    h2 Erreur
    
    if message
      p.error-message(style="margin: 20px 0; color: #d9534f; font-size: 1.2em;")= message
    else
      p.error-message(style="margin: 20px 0; color: #d9534f; font-size: 1.2em;") Une erreur s'est produite.
    
    p.error-actions(style="margin-top: 30px;")
      a(href="/", style="margin-right: 15px;") Retour à l'accueil
      a(href="/spots") Voir tous les spots